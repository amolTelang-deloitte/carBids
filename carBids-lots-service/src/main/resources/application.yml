spring:
  application:
    name: CARBIDS-LOTS-SERVICE

  jpa:
    hibernate:
      ddl-auto: create-update

  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  instance:
    prefer-ip-address: true

  client:
    service-url:
      defaultZone: http://localhost:8765/eureka/

server:
  port: 8093